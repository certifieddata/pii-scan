{
  "name": "@certifieddata/pii-scan",
  "version": "0.1.0",
  "description": "Local PII risk scanner for datasets — identifies columns likely containing personal data before synthetic generation",
  "type": "module",
  "bin": {
    "pii-scan": "dist/cli.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test",
    "lint": "node -e \"process.exit(0)\""
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  },
  "keywords": ["certifieddata", "pii", "privacy", "scanner", "synthetic-data", "cli"],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/certifieddata/pii-scan.git"
  }
}
